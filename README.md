# TypeTrack Desktop

TypeTrack desktop typing practice app by Vineet Sharma.

## Run
npm install
npm start

## Build Windows
npm run build

The release folder contains the Windows installer and portable build.

## Important
This version includes a hardened Electron startup script that disables hardware acceleration and shows a useful error if the local HTML cannot load.
